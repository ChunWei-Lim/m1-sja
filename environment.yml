name: assignment_app
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - pip
  - pip:
      - streamlit
      - plotly
      - duckdb==1.1.3
      - duckdb-engine==0.15.0